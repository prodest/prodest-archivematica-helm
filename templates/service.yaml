apiVersion: v1
kind: Service
metadata:
  name: {{ include "archivematica.fullname" . }}
  labels:
    {{- include "archivematica.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "archivematica.selectorLabels" . | nindent 4 }}