# Configurações para o Redis
redis:
  enabled: true
  image:
    repository: redis
    tag: "6.0"
    pullPolicy: IfNotPresent

  # Persistência para o Redis
  persistence:
    enabled: true
    size: 2Gi
    storageClass: ""
    accessMode: ReadWriteOnce
